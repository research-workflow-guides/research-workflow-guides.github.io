---
layout: layouts/base.njk
title: 시작하기
description: 첫 compile 성공까지 이어지는 시작 단계입니다.
lang: ko
pageClass: page-section
permalink: /ko/initial-setup/
toc:
  - id: installation
    label: 설치
  - id: project-template
    label: 프로젝트 템플릿
---
{% set pageUi = canonical.pageUi[page.url] %}
{% set breadcrumbs = pageUi.breadcrumbs %}
{% set installationDocs = canonical.groupedDocs.ko["initial-setup"].installation %}
{% set projectTemplateDocs = canonical.groupedDocs.ko["initial-setup"]["project-template"] %}
<main class="content-shell">
  {% include "components/left-rail.njk" %}

  <article class="doc-content panel" data-pagefind-body>
    {% include "components/breadcrumbs.njk" %}

    <section class="doc-hero doc-hero-simple">
      <div>
        <p class="eyebrow">1단계</p>
        <h1>시작하기</h1>
        <p class="lead">세 단계를 따라 LaTeX 환경을 구성하고 첫 번째 빌드를 성공시킵니다.</p>
      </div>
    </section>

    <section id="installation" class="doc-section">
      <h2>설치</h2>
      <div class="feature-grid feature-grid-compact">
        {% for doc in installationDocs %}
          <a class="feature-card panel" href="{{ doc.url }}">
            <p class="card-kicker">{% if doc.status == "optional" %}선택{% else %}{{ doc.order }}단계{% endif %}</p>
            <h3>{{ doc.title }}</h3>
            <p>{{ doc.description }}</p>
          </a>
        {% endfor %}
      </div>
    </section>

    <section id="project-template" class="doc-section">
      <h2>프로젝트 템플릿</h2>
      <div class="feature-grid feature-grid-compact">
        {% for doc in projectTemplateDocs %}
          <a class="feature-card panel" href="{{ doc.url }}">
            <p class="card-kicker">3단계</p>
            <h3>{{ doc.title }}</h3>
            <p>{{ doc.description }}</p>
          </a>
        {% endfor %}
      </div>
    </section>

    <section class="callout-card">
      <p class="eyebrow">다음</p>
      <h2>다음은 VS Code에서 작성하기</h2>
      <p>첫 빌드가 성공하면 VS Code의 일상적인 편집 워크플로우로 넘어갑니다.</p>
      <a class="text-link" href="/ko/writing-in-vs-code/">VS Code에서 작성하기로 이동</a>
    </section>
  </article>
</main>
